#include "main.h"
/**
 * add - a function.
 * Description : this program is an addition function
 * @i: is the type of the parametere
 * @j: is the 2nd type of the parameter
 * Return: it return a variable of type in on success
 */
int add(int i, int j)
{
	int k;

	k = i + j;
	return (k);
}
