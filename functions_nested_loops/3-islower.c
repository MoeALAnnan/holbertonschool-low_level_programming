#include "main.h"
/**
 * _islower - check if lowercase.
 * Description : this program is a checker
 * @c: int
 * Return: 1 success
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else
return (0);
}
