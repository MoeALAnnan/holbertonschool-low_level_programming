#include "main.h"
/**
 * _isupper - check if uppercase.
 * Description : this program is a checker
 * @c: int
 * Return: 1 success 0 otherwise
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
return (0);
}
