#include "main.h"
/**
 * _isdigit - check if lowercase.
 * Description : this program is a checker
 * @c: int
 * Return: 1 success 0 otherwise
 */
int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
return (0);
}
